/**
 * @desc 组件演示 - {{componentName}}
 * @author {{author}}
 * @date {{date}}
 */

import "./index.less";
import React from "react";
import { render } from "react-dom";
import {{componentName}} from "@components/{{componentName}}";
import PageWrapper from "@src/components/PageWrapper";

class Preview{{componentName}} extends React.Component {

  constructor(props) {
    super(props);
  }

  componentDidMount() {

  }

  render() {
    return (
      <PageWrapper
        comp="{{componentName}}"
        className="{{previewClassName}}"
      >
        <{{componentName}} />
      </PageWrapper>
    )
  }
}

render(<Preview{{componentName}} />, document.getElementById("app"));

if (module.hot) {
  module.hot.accept();
}